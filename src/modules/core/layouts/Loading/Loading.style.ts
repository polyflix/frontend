import { styled } from '@mui/material'

export const LoadingRootStyle = styled('div')({
  display: 'flex',
  height: '100vh',
  alignItems: 'center',
  justifyContent: 'center',
  overflow: 'hidden',
})
