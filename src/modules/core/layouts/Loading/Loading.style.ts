import { styled } from '@mui/system'

export const LoadingRootStyle = styled('div')({
  display: 'flex',
  height: '100vh',
  alignItems: 'center',
  justifyContent: 'center',
  overflow: 'hidden',
})
