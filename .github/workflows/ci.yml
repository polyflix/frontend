name: CI

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  frontend:
    uses: polyflix/.github/.github/workflows/frontend-v2.yml@main
    secrets: inherit
